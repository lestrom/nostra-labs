{
  "name": "nostra-labs",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm build:characters && pnpm build:contracts",
    "dev": "pnpm -r dev",
    "build:characters": "pnpm --filter characters build",
    "build:contracts": "pnpm --filter contracts build"
  },
  "description": "Nostra monorepo",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
